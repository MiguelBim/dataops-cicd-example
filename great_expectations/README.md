This folder includes a minimal Great Expectations suite and a checkpoint.
To run locally, you may need to `great_expectations init` once to create uncommitted configs.
In CI we run the checkpoint directly which works with in-repo suite and pandas filesystem datasource.
